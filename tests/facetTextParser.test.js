const { wordSearch, booleanSearch } = require("../public/facetTextParser");

describe("wordSearch", () => {

    it("Returns correct bounding boxes", () => {
        expect(true).toEqual(true)
    })

})